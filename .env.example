# ─── Database ────────────────────────────────────────────────────────────────
# Neon PostgreSQL connection string (asyncpg driver, SSL required)
# Format: postgresql+asyncpg://USER:PASSWORD@HOST/DBNAME?ssl=require
DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@ep-your-branch.region.aws.neon.tech/neondb?ssl=require

# ─── Auth (JWT) ───────────────────────────────────────────────────────────────
# Generate a strong secret key"
JWT_SECRET_KEY=your-super-secret-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=30

# ─── AI / LLM ─────────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-your-openai-api-key
GROQ_API_KEY=gsk_your-groq-api-key

# Primary LLM (Groq) and fallback (OpenAI)
PRIMARY_LLM=llama-3.3-70b-versatile
BACKUP_LLM=gpt-4o-mini

# Embedding model (OpenAI)
EMBEDDING_MODEL=text-embedding-3-small

# ─── Cloudflare R2 (image storage) ────────────────────────────────────────────
R2_ENDPOINT_URL=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com/
R2_ACCESS_KEY=your-r2-access-key-id
R2_SECRET_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=your-bucket-name
R2_PUBLIC_URL=https://your-public-domain.example.com
